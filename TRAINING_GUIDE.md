# 📚 Training Guide - คู่มือเทรนโมเดลแบบละเอียด

คู่มือฉบับสมบูรณ์สำหรับการเทรนโมเดลทำนายราคาหุ้น ครอบคลุมทั้ง 4 วิธี พร้อมเคล็ดลับและการแก้ปัญหา

---

## 📑 สารบัญ

- [🎯 ภาพรวม](#-ภาพรวม)
- [🚀 Quick Start](#-quick-start)
- [📖 วิธีเทรนทั้ง 4 แบบ](#-วธเทรนทง-4-แบบ)
  - [1. Command Line (CLI)](#1-command-line-cli)
  - [2. JupyterLab Notebook (UI)](#2-jupyterlab-notebook-ui)
  - [3. Airflow Automation](#3-airflow-automation)
  - [4. Python Script](#4-python-script-โดยตรง)
- [⚡ Rate Limiting & Retry Logic](#-rate-limiting--retry-logic)
- [🔮 การพยากรณ์อนาคต](#-การพยากรณอนาคต)
- [📊 การเปรียบเทียบโมเดล](#-การเปรยบเทยบโมเดล)
- [💡 Best Practices](#-best-practices)
- [🚨 Troubleshooting](#-troubleshooting)
- [❓ FAQ](#-faq)

---

## 🎯 ภาพรวม

### ระบบเทรนโมเดล

ระบบนี้รองรับการเทรนโมเดล Machine Learning 3 ประเภท:

| โมเดล | คุณสมบัติ | MAPE (AAPL) | แนะนำ |
|-------|-----------|-------------|-------|
| **LSTM** | Long Short-Term Memory, เหมาะกับ time series | 4.17% | ⭐⭐ |
| **GRU** | Gated Recurrent Unit, เร็วกว่า LSTM | 3.42% | ⭐⭐⭐ **ดีที่สุด** |
| **Transformer** | Attention mechanism, ต้องการข้อมูลมาก | 31.97% | ⭐ |

### 4 วิธีในการเทรน

| วิธี | ความเร็ว | ความยาก | เหมาะกับ | การใช้งาน |
|------|----------|---------|----------|-----------|
| **CLI** | ⚡⚡⚡ | ⭐ | Production, Batch | `make train-aapl` |
| **UI** | ⚡⚡ | ⭐ | Data Scientists | `make open-jupyter` |
| **Airflow** | ⚡⚡ | ⭐⭐ | Scheduled jobs | `make airflow-trigger` |
| **Python** | ⚡⚡⚡ | ⭐⭐⭐ | Custom scripts | Python API |

---

## 🚀 Quick Start

### ขั้นตอนแรก - Setup ระบบ

```bash
# 1. Start all services
make all

# 2. Wait for services to be ready
make wait

# 3. Check status
make check
```

### ขั้นตอนที่สอง - เทรนทดสอบ

```bash
# ทดสอบเร็ว (AAPL, LSTM, 2023-2024) - ใช้เวลา 5-10 นาที
make train-test-quick
```

### ขั้นตอนที่สาม - เทรนจริง

```bash
# เทรนบริษัทเดียว (ทั้ง 3 โมเดล)
make train-aapl

# หรือหลายบริษัท
make train-tech
```

---

## 📖 วิธีเทรนทั้ง 4 แบบ

---

## 1. Command Line (CLI)

### ✨ ข้อดี
- ⚡ **เร็วที่สุด** - รันคำสั่งเดียวจบ
- 🎯 **แม่นยำ** - ควบคุมได้ทุกอย่าง
- 📋 **ง่าย** - จำแค่ `make train-{ticker}`
- 🔄 **Scriptable** - เขียน script automation ได้

### ❌ ข้อเสีย
- 🚫 ไม่มี UI - ดูผลแบบ real-time ไม่ได้
- 🚫 ต้องรอจบ - ไม่สามารถแก้ไขระหว่างทางได้

---

### 📋 คำสั่งพื้นฐาน

#### เทรนบริษัทเดียว (ทั้ง 3 โมเดล)

```bash
make train-tsla     # Tesla
make train-aapl     # Apple
make train-googl    # Google
make train-nvda     # NVIDIA
make train-msft     # Microsoft
```

**สิ่งที่เกิดขึ้น:**
1. Download ข้อมูลจาก Yahoo Finance (yahooquery)
2. เทรนโมเดลทั้ง 3: LSTM, GRU, Transformer
3. บันทึกโมเดล + scaler + metrics
4. สร้างกราฟ prediction

**ระยะเวลา:** 30-60 นาที (ขึ้นกับข้อมูล)

---

#### เทรนหลายบริษัทพร้อมกัน

```bash
# Tech Giants (5 บริษัท)
make train-tech              # TSLA, AAPL, GOOGL, MSFT, NVDA

# Semiconductors (4 บริษัท)
make train-semiconductor     # NVDA, AMD, INTC, TSM

# FAANG (5 บริษัท)
make train-faang             # META, AAPL, AMZN, NFLX, GOOGL

# All Default (5 บริษัท)
make train-all-default       # TSLA, AAPL, GOOGL, MSFT, AMZN
```

**ระยะเวลา:** 2-5 ชั่วโมง (ขึ้นกับจำนวนบริษัท)

**💡 Tips:**
- รันตอนกลางคืน หรือใช้ `tmux`/`screen`
- มี retry logic อัตโนมัติ ไม่ต้องกังวล rate limit
- ถ้าบริษัทใดล้มเหลว จะข้ามไปบริษัทถัดไป

---

#### เทรนเฉพาะโมเดล (เร็วกว่า)

```bash
make train-lstm-only         # เฉพาะ LSTM (~10 นาที)
make train-gru-only          # เฉพาะ GRU (~10 นาที)
make train-transformer-only  # เฉพาะ Transformer (~15 นาที)
```

**เมื่อไหร่ใช้:**
- ต้องการเทรนเร็ว
- ทดสอบโมเดลใหม่
- รู้แล้วว่าโมเดลไหนดีที่สุด

---

#### ทดสอบเร็ว

```bash
# Quick test (AAPL, LSTM, 2023-2024)
make train-test-quick        # ใช้เวลา 5-10 นาที

# Recent data (TSLA, ALL, 2023-2024)
make train-test-recent       # ใช้เวลา 15-30 นาที
```

**แนะนำ:**
- รันครั้งแรกเพื่อทดสอบระบบ
- ตรวจสอบว่าทุกอย่างทำงานได้

---

### 🔧 คำสั่งขั้นสูง (ใช้ train_stock.sh)

```bash
# รูปแบบ: ./train_stock.sh TICKER MODEL START_DATE [END_DATE]

# ตัวอย่าง 1: เทรนทุกโมเดล
./train_stock.sh AAPL ALL 2018-01-01

# ตัวอย่าง 2: เทรนเฉพาะ GRU
./train_stock.sh TSLA GRU 2020-01-01

# ตัวอย่าง 3: กำหนดช่วงเวลา
./train_stock.sh GOOGL ALL 2019-01-01 2024-12-31

# ตัวอย่าง 4: ข้อมูลล่าสุด (ไม่ใส่ END_DATE)
./train_stock.sh NVDA ALL 2018-01-01
```

**Parameters:**
- `TICKER`: รหัสหุ้น (AAPL, TSLA, etc.)
- `MODEL`: โมเดล (ALL, LSTM, GRU, TRANSFORMER)
- `START_DATE`: วันเริ่มต้น (YYYY-MM-DD)
- `END_DATE`: วันสิ้นสุด (ไม่ใส่ = วันล่าสุด)

---

### 📊 ดูผลลัพธ์

```bash
# ดู metrics
make view-tsla

# ดูโมเดลที่เทรนแล้ว
make models-list
make models-list-all

# เปรียบเทียบโมเดล
make compare-aapl

# ดูกราฟ (macOS)
make view-plots
```

---

## 2. JupyterLab Notebook (UI)

### ✨ ข้อดี
- 🎨 **UI สวยงาม** - เห็นภาพชัดเจน
- 🔄 **Real-time** - เห็นความคืบหน้าทันที
- 📊 **Visualization** - กราฟในหน้าเดียว
- 👥 **User-friendly** - เหมาะกับ Data Scientists

### ❌ ข้อเสีย
- 🐌 ช้ากว่า CLI เล็กน้อย
- 💻 ต้องเปิด browser

---

### 🚀 เริ่มต้นใช้งาน

#### Step 1: เปิด JupyterLab

```bash
# วิธีที่ 1: เปิด browser อัตโนมัติ
make open-jupyter

# วิธีที่ 2: เปิดเอง
# URL:   http://localhost:8888
# Token: fintech2025
# File:  notebooks/train_stocks_ui.ipynb
```

---

#### Step 2: แก้ไข Configuration

เปิดไฟล์ `train_stocks_ui.ipynb` แล้วแก้ Cell แรก:

```python
# ============================================================
# CONFIGURATION - แก้ตรงนี้!
# ============================================================

# บริษัทที่ต้องการเทรน
TICKER = 'AAPL'          # หรือ 'TSLA', 'GOOGL', 'NVDA', 'MSFT'

# โมเดล
MODEL_TYPE = 'ALL'       # หรือ 'LSTM', 'GRU', 'TRANSFORMER'

# ช่วงเวลา
START_DATE = '2018-01-01'
END_DATE = None          # None = ใช้วันล่าสุด

# Hyperparameters
EPOCHS = 50
BATCH_SIZE = 32
```

---

#### Step 3: รัน Cells

**เทรนบริษัทเดียว:**
```python
# Cell: Train Single Company
from train_multi_company import StockModelTrainer

trainer = StockModelTrainer(
    ticker=TICKER,
    model_type=MODEL_TYPE,
    start_date=START_DATE,
    end_date=END_DATE
)

success = trainer.run(epochs=EPOCHS, batch_size=BATCH_SIZE)

if success:
    print(f"✅ Training completed for {TICKER}!")
else:
    print(f"❌ Training failed for {TICKER}")
```

**เทรนหลายบริษัท:**
```python
# Cell: Train Multiple Companies
tickers = ['TSLA', 'AAPL', 'GOOGL', 'NVDA', 'MSFT']

for ticker in tickers:
    print(f"\n{'='*60}")
    print(f"Training {ticker}...")
    print(f"{'='*60}\n")

    trainer = StockModelTrainer(
        ticker=ticker,
        model_type='ALL',
        start_date='2018-01-01'
    )

    trainer.run(epochs=50, batch_size=32)
```

---

#### Step 4: ดูผลลัพธ์

```python
# Cell: View Results
import pickle
import matplotlib.pyplot as plt

# Load metrics
ticker = 'AAPL'
for model in ['lstm', 'gru', 'transformer']:
    path = f'models/{ticker}/{model}_{ticker.lower()}_metrics.pkl'

    with open(path, 'rb') as f:
        metrics = pickle.load(f)

    print(f"\n{model.upper()}:")
    print(f"  RMSE: ${metrics['rmse']:.2f}")
    print(f"  MAE:  ${metrics['mae']:.2f}")
    print(f"  MAPE: {metrics['mape']:.2f}%")

# Show plots
from IPython.display import Image, display

for model in ['lstm', 'gru', 'transformer']:
    plot_path = f'models/{ticker}/{model}_{ticker.lower()}_prediction.png'
    display(Image(filename=plot_path))
```

---

### 📊 Features ของ Notebook

1. **Configuration Cells** - ปรับค่าได้ง่าย
2. **Progress Display** - เห็นความคืบหน้า real-time
3. **Auto Retry** - จัดการ rate limit อัตโนมัติ
4. **Visualization** - แสดงกราฟทันที
5. **Metrics Summary** - สรุปผลเปรียบเทียบ

---

### 💡 Tips สำหรับ UI

```python
# 1. ทดสอบก่อนด้วยข้อมูลน้อย
START_DATE = '2023-01-01'
EPOCHS = 10

# 2. เทรนเฉพาะโมเดลที่ต้องการ
MODEL_TYPE = 'GRU'  # เร็วกว่า ALL

# 3. ใช้ Multiple Companies สำหรับ batch
tickers = ['AAPL', 'TSLA', 'GOOGL']

# 4. บันทึก notebook เมื่อเสร็จ
# File > Save Notebook
```

---

## 3. Airflow Automation

### ✨ ข้อดี
- ⏰ **Schedule** - รันอัตโนมัติทุกวัน
- 📊 **Monitoring** - เห็นสถานะแต่ละ Task
- 🔁 **Retry** - Retry อัตโนมัติเมื่อล้มเหลว
- 🏭 **Production-ready** - เหมาะกับ production

### ❌ ข้อเสีย
- 🔧 Setup ซับซ้อนกว่า
- 📚 ต้องเข้าใจ Airflow DAG

---

### 🚀 เริ่มต้นใช้งาน

#### Step 1: เปิด Airflow Web UI

```bash
# เปิด browser
open http://localhost:8083

# Login
Username: admin
Password: fintech2025
```

---

#### Step 2: เปิด DAG

1. ค้นหา DAG: `multi_company_stock_training`
2. คลิก **Toggle** เพื่อเปิด DAG
3. DAG จะรันทุกวันเวลา 6:00 AM

---

#### Step 3: Trigger Manual (On-Demand)

**ผ่าน Web UI:**
1. คลิก DAG name: `multi_company_stock_training`
2. คลิกปุ่ม **Trigger DAG** (เครื่องหมายเล่น ▶)
3. ดูความคืบหน้าใน **Graph View**

**ผ่าน Command:**
```bash
make airflow-trigger
```

---

### 📊 DAG Configuration

**DAG นี้เทรนบริษัทเหล่านี้:**
- TSLA (Tesla)
- AAPL (Apple)
- GOOGL (Google)
- MSFT (Microsoft)
- AMZN (Amazon)

**Schedule:**
- รันทุกวันเวลา 6:00 AM
- Timezone: UTC

**Retry:**
- Retry 3 ครั้งถ้าล้มเหลว
- รอ 5 นาทีระหว่าง retry

---

### 🔧 แก้ไข DAG

แก้ไขไฟล์: `airflow/dags/multi_company_stock_training_dag.py`

```python
# เปลี่ยนบริษัทที่เทรน
tickers = ['TSLA', 'AAPL', 'NVDA']  # แก้ตรงนี้

# เปลี่ยน schedule
schedule_interval='0 6 * * *'  # 6:00 AM ทุกวัน
# schedule_interval='0 */6 * * *'  # ทุก 6 ชั่วโมง
# schedule_interval='@weekly'  # ทุกสัปดาห์

# เปลี่ยนช่วงเวลาข้อมูล
start_date = '2020-01-01'  # แก้ตรงนี้
```

---

### 📊 ดู Logs

```bash
# ดู Airflow logs
make airflow-logs

# ดูสถานะ
make airflow-status

# ดู DAGs ทั้งหมด
make airflow-list
```

---

## 4. Python Script โดยตรง

### ✨ ข้อดี
- 🔧 **ยืดหยุ่นสูงสุด** - ปรับค่าได้ทุกอย่าง
- 🎯 **ควบคุมเต็มที่** - เขียน logic custom ได้
- 🐍 **Python Native** - เรียกจาก script อื่นได้
- 🧪 **เหมาะกับ Development**

### ❌ ข้อเสีย
- 💻 ต้องเข้าใจ Python
- 🐚 ต้องเข้า container

---

### 🚀 เริ่มต้นใช้งาน

#### Step 1: เข้า Container

```bash
make shell-jupyter
```

---

#### Step 2: รัน Script

**รูปแบบพื้นฐาน:**
```bash
python scripts/stock_prediction/train_multi_company.py \
    --ticker AAPL \
    --model ALL \
    --start-date 2018-01-01
```

**ตัวอย่างต่างๆ:**

```bash
# 1. เทรนทุกโมเดล
python scripts/stock_prediction/train_multi_company.py \
    --ticker TSLA \
    --model ALL \
    --start-date 2018-01-01

# 2. เทรนเฉพาะ GRU
python scripts/stock_prediction/train_multi_company.py \
    --ticker AAPL \
    --model GRU \
    --start-date 2020-01-01

# 3. กำหนดช่วงเวลา
python scripts/stock_prediction/train_multi_company.py \
    --ticker GOOGL \
    --model ALL \
    --start-date 2019-01-01 \
    --end-date 2024-12-31

# 4. ปรับ hyperparameters
python scripts/stock_prediction/train_multi_company.py \
    --ticker NVDA \
    --model GRU \
    --start-date 2018-01-01 \
    --epochs 100 \
    --batch-size 64
```

---

#### Step 3: ใช้เป็น Python Module

```python
import sys
sys.path.insert(0, 'scripts/stock_prediction')

from train_multi_company import StockModelTrainer

# Create trainer
trainer = StockModelTrainer(
    ticker='AAPL',
    model_type='GRU',
    start_date='2018-01-01',
    end_date=None  # None = latest
)

# Train
success = trainer.run(epochs=50, batch_size=32)

if success:
    print(f"✅ Training completed!")

    # Get metrics
    metrics = trainer.get_metrics()
    print(f"RMSE: ${metrics['rmse']:.2f}")
    print(f"MAPE: {metrics['mape']:.2f}%")
```

---

### 📝 Parameters

| Parameter | Type | Description | Default |
|-----------|------|-------------|---------|
| `--ticker` | str | รหัสหุ้น (AAPL, TSLA, etc.) | **Required** |
| `--model` | str | โมเดล (ALL, LSTM, GRU, TRANSFORMER) | **Required** |
| `--start-date` | str | วันเริ่มต้น (YYYY-MM-DD) | **Required** |
| `--end-date` | str | วันสิ้นสุด (YYYY-MM-DD) | None (latest) |
| `--epochs` | int | จำนวน epochs | 50 |
| `--batch-size` | int | Batch size | 32 |

---

## ⚡ Rate Limiting & Retry Logic

### ปัญหา: Yahoo Finance Rate Limit

Yahoo Finance จำกัดการเรียก API:
- ~100-200 requests/hour (2025-2026)
- Error: `429 Too Many Requests`

---

### 🛡️ วิธีแก้ (อัตโนมัติ)

#### 1. ใช้ yahooquery เป็นหลัก

```python
# ระบบใช้ yahooquery ก่อน (ไม่ติด rate limit บ่อย)
from yahooquery import Ticker

ticker_obj = Ticker('AAPL')
df = ticker_obj.history(start='2018-01-01', end='2024-12-31')
```

#### 2. Fallback ไป yfinance

```python
# ถ้า yahooquery ล้มเหลว จะใช้ yfinance
import yfinance as yf

df = yf.download('AAPL', start='2018-01-01', end='2024-12-31')
```

#### 3. Retry Logic

```python
# ลองใหม่ 5 ครั้ง ด้วย exponential backoff
max_retries = 5
wait_times = [5, 10, 20, 40, 80]  # วินาที

for attempt in range(max_retries):
    try:
        df = download_data()
        break
    except Exception as e:
        if attempt < max_retries - 1:
            time.sleep(wait_times[attempt])
            continue
        else:
            raise
```

---

### 💡 เคล็ดลับหลีกเลี่ยง Rate Limit

1. **รอระหว่างเทรนหลายบริษัท**
   ```python
   time.sleep(3)  # รอ 3 วินาทีระหว่างบริษัท
   ```

2. **เทรนตอนกลางคืน**
   ```bash
   # ใช้ Airflow schedule
   schedule_interval='0 2 * * *'  # 2:00 AM
   ```

3. **ใช้ข้อมูลที่ cache ไว้**
   ```bash
   # ถ้าเทรนซ้ำ ระบบจะใช้ข้อมูลเดิม
   ```

4. **เทรนทีละบริษัท**
   ```bash
   make train-aapl
   # รอ 5-10 นาที
   make train-tsla
   ```

---

## 🔮 การพยากรณ์อนาคต

### พยากรณ์ด้วยโมเดลที่เทรนแล้ว

```bash
# ต้องเทรนโมเดลก่อน!
make train-aapl
```

---

### 🎯 คำสั่งพยากรณ์

#### แบบง่าย (ใช้โมเดล GRU)

```bash
# พยากรณ์ 30 วัน
make predict-aapl-day
make predict-tsla-day
make predict-day TICKER=GOOGL

# พยากรณ์ 4 สัปดาห์
make predict-aapl-week
make predict-week TICKER=NVDA

# พยากรณ์ 3 เดือน
make predict-aapl-month
make predict-month TICKER=MSFT

# พยากรณ์ 1 ปี
make predict-aapl-year
make predict-year TICKER=AMZN
```

---

#### แบบกำหนดเอง (เลือกโมเดล)

```bash
# รูปแบบ: make predict-custom TICKER=XXX MODEL=YYY PERIODS=N TYPE=ZZZ

# ตัวอย่าง 1: ใช้โมเดล LSTM
make predict-custom TICKER=AAPL MODEL=LSTM PERIODS=30 TYPE=day

# ตัวอย่าง 2: ใช้โมเดล GRU (แนะนำ)
make predict-custom TICKER=AAPL MODEL=GRU PERIODS=3 TYPE=month

# ตัวอย่าง 3: ใช้โมเดล Transformer
make predict-custom TICKER=TSLA MODEL=TRANSFORMER PERIODS=60 TYPE=day

# ตัวอย่าง 4: พยากรณ์ 6 เดือน
make predict-custom TICKER=GOOGL MODEL=GRU PERIODS=6 TYPE=month
```

**Parameters:**
- `TICKER` - รหัสหุ้น (AAPL, TSLA, etc.)
- `MODEL` - โมเดล (LSTM, GRU, TRANSFORMER)
- `PERIODS` - จำนวนช่วงเวลา (default: 30)
- `TYPE` - ประเภท (day, week, month, year)

---

### 📊 ผลลัพธ์ที่ได้

```
============================================================
PREDICTION SUMMARY
============================================================

📍 Current Price:    $259.37
📅 Current Date:     2024-12-31

🔮 First Prediction: $256.12
📅 Prediction Start: 2025-01-02

🎯 Final Prediction: $181.87
📅 Prediction End:   2025-01-31

📈 Expected Change:  -$77.50 (-29.88%)
🔴 Trend: BEARISH (Downward)

📊 First 5 Predictions:
------------------------------------------------------------
   2025-01-02: $256.12 (-$3.25, -1.25%)
   2025-01-03: $253.45 (-$5.92, -2.28%)
   2025-01-06: $250.87 (-$8.50, -3.28%)
   ...

⚠️  DISCLAIMER:
   This is a prediction based on historical data.
   Model Accuracy: RMSE $6.73, MAPE 3.42%
   Actual prices may vary significantly.
   Not financial advice. Do your own research!
============================================================
```

---

### 🖼️ กราฟที่ได้

บันทึกใน: `models/{TICKER}/gru_{ticker}_future_prediction.png`

แสดง:
- กราฟราคาจริง 60 วันล่าสุด (เส้นสีน้ำเงิน)
- กราฟราคาพยากรณ์ (เส้นสีแดงประ)
- จุดเชื่อมต่อ (เส้นสีส้มประ)
- ข้อมูลสรุป (กล่องข้อความ)

---

## 📊 การเปรียบเทียบโมเดล

### เปรียบเทียบ LSTM vs GRU vs Transformer

```bash
# เปรียบเทียบทุกบริษัท
make compare-all

# เปรียบเทียบเฉพาะบริษัท
make compare-aapl
make compare-tsla
make compare-googl
```

---

### 📊 ผลลัพธ์

```
================================================================================
MODEL COMPARISON FOR AAPL
================================================================================

📊 PERFORMANCE METRICS:
--------------------------------------------------------------------------------
  Model        RMSE     MAE    MAPE  Trained         Train Samples  Test Samples
  LSTM         8.28    7.98    4.17  2025-01-12               1412           354
  GRU          6.73    6.54    3.42  2025-01-12               1412           354
  TRANSFORMER 61.33   61.17   31.97  2025-01-12               1412           354
--------------------------------------------------------------------------------

🏆 RANKINGS:
--------------------------------------------------------------------------------
Best RMSE:  GRU ($6.73)
Best MAE:   GRU ($6.54)
Best MAPE:  GRU (3.42%)

📈 OVERALL RANKING (Average across all metrics):
--------------------------------------------------------------------------------
1. GRU         ⭐⭐⭐
2. LSTM        ⭐⭐
3. TRANSFORMER ⭐
--------------------------------------------------------------------------------

💡 RECOMMENDATION:
   Use GRU model for AAPL
   - Best overall performance (Avg Rank: 1.00)
   - RMSE: $6.73
   - MAE: $6.54
   - MAPE: 3.42%

📊 PERFORMANCE DIFFERENCES:
--------------------------------------------------------------------------------
Best vs Worst model:
  RMSE difference: 811.3%
  MAE difference:  835.3%
  MAPE difference: 835.1%

================================================================================
```

---

### 📈 กราฟเปรียบเทียบ

บันทึกใน: `models/{TICKER}/comparison_{ticker}.png`

แสดง:
- 3 กราฟแท่งเปรียบเทียบ RMSE, MAE, MAPE
- Highlight โมเดลที่ดีที่สุดด้วยสีทอง
- แสดงค่าบนแท่ง

---

## 💡 Best Practices

### 1. 🚀 เริ่มต้นด้วย Quick Test

```bash
# ทดสอบระบบก่อน (5-10 นาที)
make train-test-quick

# ถ้าผ่าน แล้วค่อยเทรนจริง
make train-aapl
```

---

### 2. 📊 เปรียบเทียบโมเดลก่อนพยากรณ์

```bash
# 1. เทรนทั้ง 3 โมเดล
make train-aapl

# 2. เปรียบเทียบ
make compare-aapl

# 3. ดูว่าโมเดลไหนดีที่สุด
# Output: "Use GRU model for AAPL"

# 4. ใช้โมเดลนั้นพยากรณ์
make predict-custom TICKER=AAPL MODEL=GRU PERIODS=30 TYPE=day
```

---

### 3. 💾 Backup ก่อนลบ

```bash
# Backup
make models-backup

# ดูขนาด
make models-size

# แล้วค่อยลบ
make models-clean
```

---

### 4. 📅 ใช้ข้อมูลย้อนหลังเพียงพอ

```bash
# แนะนำ: อย่างน้อย 5-7 ปี
START_DATE='2018-01-01'  # ✅ ดี

# หลีกเลี่ยง: ข้อมูลน้อยเกินไป
START_DATE='2023-01-01'  # ⚠️ อาจไม่แม่นยำ
```

---

### 5. 🎯 ใช้ GRU สำหรับความแม่นยำสูงสุด

GRU ให้ผลลัพธ์ดีที่สุดในการทดสอบ:
- MAPE: 3.42% (LSTM: 4.17%, Transformer: 31.97%)
- เร็วกว่า LSTM
- ใช้หน่วยความจำน้อยกว่า

```bash
# แนะนำ
make train-gru-only
make predict-custom TICKER=AAPL MODEL=GRU PERIODS=30 TYPE=day
```

---

### 6. ⚠️ พยากรณ์ระยะสั้นแม่นยำกว่า

| ระยะเวลา | ความแม่นยำ | แนะนำ |
|----------|-----------|-------|
| 1-30 วัน | ⭐⭐⭐ | ใช้ได้ |
| 1-3 เดือน | ⭐⭐ | ใช้ระวัง |
| 6-12 เดือน | ⭐ | ดูแนวโน้มเท่านั้น |

```bash
# แนะนำ
make predict-aapl-day        # 30 วัน
make predict-aapl-week       # 4 สัปดาห์

# ระยะยาว (uncertainty สูง)
make predict-aapl-year       # ใช้ดูแนวโน้มเท่านั้น
```

---

### 7. 📜 ดู Logs เมื่อมีปัญหา

```bash
# ดู JupyterLab logs
make logs-jupyter

# ดูสถานะ
make check
make ps

# เข้า container debug
make shell-jupyter
```

---

### 8. 🔄 Update โมเดลเป็นประจำ

```bash
# เทรนใหม่ทุกสัปดาห์/เดือน
make train-aapl

# หรือใช้ Airflow schedule
# DAG จะรันอัตโนมัติทุกวันเวลา 6:00 AM
```

---

## 🚨 Troubleshooting

### ❌ Yahoo Finance Rate Limit (429 Error)

**อาการ:**
```
429 Client Error: Too Many Requests
Failed to get ticker 'AAPL' reason: ...
```

**วิธีแก้:**
1. ระบบมี retry อัตโนมัติแล้ว - รอสักครู่
2. ถ้ายังไม่ได้ ให้รอ 5-10 นาที แล้วลองใหม่
3. ใช้ yahooquery (ระบบใช้อยู่แล้ว)

```bash
# ลองใหม่
make train-aapl
```

---

### ❌ Container ไม่ทำงาน

**อาการ:**
```
Error: JupyterLab container is not running!
```

**วิธีแก้:**
```bash
# ตรวจสอบสถานะ
make ps

# Start services
make up
make wait

# ถ้ายังไม่ได้
make restart
```

---

### ❌ เทรนล้มเหลว

**อาการ:**
```
Training failed for AAPL
```

**วิธีแก้:**
```bash
# 1. เช็ค syntax
make check-syntax

# 2. ทดสอบ yfinance
make test-yfinance

# 3. เข้า container debug
make shell-jupyter
python scripts/stock_prediction/train_multi_company.py --help
```

---

### ❌ โมเดลไม่เจอ

**อาการ:**
```
Model not found: models/AAPL/gru_aapl_model.h5
Train first: make train-aapl
```

**วิธีแก้:**
```bash
# ดูว่ามีโมเดลอะไรบ้าง
make models-list

# ถ้าไม่มี ให้เทรนใหม่
make train-aapl
```

---

### ❌ พยากรณ์ล้มเหลว

**อาการ:**
```
Model not found for prediction
```

**วิธีแก้:**
```bash
# 1. เช็คว่าโมเดลถูกเทรนแล้ว
make models-list

# 2. ถ้ายังไม่ได้เทรน
make train-aapl

# 3. แล้วลองพยากรณ์ใหม่
make predict-aapl-day
```

---

### ❌ Out of Memory

**อาการ:**
```
OOM when allocating tensor
```

**วิธีแก้:**
```bash
# ลด batch size
./train_stock.sh AAPL GRU 2018-01-01 --batch-size 16

# หรือเทรนทีละโมเดล
make train-gru-only
```

---

## ❓ FAQ

### Q1: ควรใช้โมเดลไหนดี?

**A:** GRU (ดีที่สุด)
- MAPE: 3.42%
- เร็วกว่า LSTM
- แม่นยำกว่า Transformer มาก

---

### Q2: ใช้เวลาเทรนนานเท่าไหร่?

**A:** ขึ้นกับโมเดลและข้อมูล
- LSTM: ~15-20 นาที
- GRU: ~10-15 นาที
- Transformer: ~20-30 นาที
- ทั้ง 3: ~30-60 นาที

---

### Q3: ทำไมถึงเจอ Rate Limit?

**A:** Yahoo Finance จำกัดการเรียก API
- ~100-200 requests/hour
- ระบบมี retry logic แล้ว
- ใช้ yahooquery เป็นหลัก (ไม่ติดบ่อย)

---

### Q4: พยากรณ์แม่นยำแค่ไหน?

**A:** ขึ้นกับโมเดลและระยะเวลา
- GRU: MAPE 3.42% (ดีที่สุด)
- ระยะสั้น (1-30 วัน) แม่นกว่าระยะยาว
- ⚠️ ไม่ใช่คำแนะนำการลงทุน!

---

### Q5: เทรนบ่อยแค่ไหนดี?

**A:** แนะนำ:
- ทุกสัปดาห์ - สำหรับ production
- ทุกเดือน - สำหรับ development
- ใช้ Airflow schedule อัตโนมัติ

---

### Q6: ใช้ข้อมูลย้อนหลังกี่ปี?

**A:** แนะนำ 5-7 ปี
- น้อยกว่า 3 ปี: อาจไม่แม่นยำ
- 5-7 ปี: สมดุล
- มากกว่า 10 ปี: ข้อมูลเก่าไม่เกี่ยวข้อง

---

### Q7: ทำไม Transformer แม่นยำต่ำ?

**A:** Transformer ต้องการข้อมูลมาก
- ข้อมูลหุ้นไม่เพียงพอ
- เหมาะกับ NLP มากกว่า time series
- ใช้ GRU แทนจะดีกว่า

---

### Q8: Backup โมเดลอย่างไร?

**A:**
```bash
make models-backup
# บันทึกไปที่: models_backup/
```

---

### Q9: ลบโมเดลเก่าอย่างไร?

**A:**
```bash
# Backup ก่อน
make models-backup

# แล้วลบ
make models-clean
```

---

### Q10: เทรนผ่าน UI หรือ CLI ดีกว่า?

**A:** ขึ้นกับความต้องการ

| สถานการณ์ | แนะนำ |
|-----------|-------|
| ครั้งแรก, ทดสอบ | UI (JupyterLab) |
| Production, Batch | CLI (Makefile) |
| Schedule อัตโนมัติ | Airflow |
| Custom, Development | Python Script |

---

## 📚 เอกสารเพิ่มเติม

- **[README.md](README.md)** - ภาพรวมทั้งระบบ
- **[MAKEFILE_GUIDE.md](MAKEFILE_GUIDE.md)** - คู่มือ Makefile

---

**Happy Training! 🚀📈**

Made with ❤️ for ducation and learning By Sojirat.S
